1. .h 文件中声明的变量可以在对应的.cpp文件中直接使用. 
	1. ``` 需要注意的是宏语句后面是不加分号的
	    UPROPERTY(EditAnyWhere)
		float MemberFloat = 10.0f;
	   ```
	![[Pasted image 20251109163901.png]]
	- 这里需要注意的是, 在vs2022 中编辑代码后,我重复忘记保存代码导致UE中没有更新
	- vs2022中保存代码后,在UE中点击底部实时编译按钮后即可更新进UE中
2. .h中声明的变量,在.cpp中可以直接进行操作.
	1. 例如 .cpp 中函数中直接使用 MemberFloat = 20.0f;
[更详细的说明](../学习文档/UE开发中的宏说明.md)

### 判断两个向量是否相等
向量1.Equals(向量2)